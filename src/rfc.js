
var StringUtilities = {
	getFirstVowel: function(word){
		
	}
	clearString: function(word) {
		string = word.trim();
	}
}

var mxk = {
	getCURP: function(name, surnameFather, surnameMother, bornDate, bornState, gender) {

		rfc = this->getCommonPart(name, surnameFather, surnameFather,bornDate);
		}

	getCommonPart: function(name, surnameFather, surnameMother, bornDate) {
		name 	= StringUtilities->clearString(name);
		surnameFather = toUpperCase(surnameFather);
		surnameMother = toUpperCase(surnameMother);

		commonPart = 
	}

}